### Récupération des informations d'un dossier depuis Démarche numérique : ❌ erreur

L'API de « Démarche numérique » a retourné une erreur lors de la récupération du dossier n°{{ dossier_number }}.

Réponse de « Démarche numérique » :
```
{{ error|safe }}
```

Requête envoyée :
* Query:
```
{{ query|safe }}
```
* Variables:
```
{{ variables|safe }}
```
