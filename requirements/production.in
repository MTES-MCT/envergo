# PRECAUTION: avoid production dependencies that aren't in development

-r base.txt

gunicorn
psycopg[binary]
sentry-sdk

# Django
# ------------------------------------------------------------------------------
django-storages[boto3]
